# 원작 검토와 Classic 복원

## 근거

`source@8959147/lib/main.dart`와 `main@b858c44/main.dart.js`를 대조했다. 기존의 ‘정확하게 치면 선명해지는 게임’이라는 평가는 불완전했다. 핵심은 저속에서 대비 손실, 고속에서 커지는 마킹의 가림, 더 넓은 마킹에서 더 높은 정답 점수라는 결합이다.

| 원작 위치 | 규칙                                                        |
| --------- | ----------------------------------------------------------- |
| 172–180   | 직전 정답과의 밀리초 간격으로 60000/간격 계산               |
| 214–221   | 매초 ceil(width - ceil(width × .25)); 작은 너비는 1         |
| 643–651   | white alpha `colSat`의 독립된 덮개                          |
| 668–676   | 실제 커서 너비, 불투명도 245/255, 너비 39 초과 시 진한 회색 |
| 698–700   | 내부 너비 250 미만이면 속도/100 더하기; 엄격한 상한 아님    |
| 706–714   | 정답 앞 글자 제거, ceil(width/10)점                         |
| 716–728   | 오타 시 ceil(score × .3) 차감                               |
| 730–745   | 정수 너비 40 이상 alpha -2, 미만 +3; 끝단 별도 처리         |
| 747–754   | alpha 갱신 후 오류 너비 1, 지문이 비면 완료                 |

## 보존한 내용

상수와 이벤트 순서, 매초의 계단식 감쇠, 입력 때만 갱신되는 흰 덮개, 앞 글자가 제거되는 문장, 실제 불투명 마킹, 지문 완료 조건을 보존했다. 오타 삭제도 원작처럼 오답 변경으로 판정한다. 고속 오타의 첫 이벤트에서 덮개가 옅어질 수 있는 순서를 유지했다.

최초 복원에서는 원작 영문 30개를 사용했다. 이후 사용자의 지문 교체·확대 요청에 따라 한글 30개와 영문 30개의 현대적인 창작 지문으로 교체했다. 판정 규칙은 그대로다. 이전 지문과 비교하면 한 판의 길이와 얻을 수 있는 총점은 달라진다. 기록에는 지문 제목이 남는다.

## 기술적 수정과 표시 차이

- 동일 밀리초 입력 간격은 최소 1ms로 처리하여 Infinity를 방지한다.
- 문자열 범위를 검사하고 확정된 NFC 한 글자만 평가한다. 붙여넣기/일괄 입력은 제한한다. UTF-16 절단 대신 Unicode code point를 사용한다.
- IME 조합 중에는 평가하지 않으며 후속 중복 콜백을 억제한다.
- 재시작 때 직전 정답 시간과 타이머를 초기화하고 컴포넌트 종료 시 타이머를 정리한다. 원본의 재시작 후 오래된 시간값/타이머 누수는 복제하지 않는다.
- 원작의 페이지 전체 확대 지문·랜덤 색상 배경은 집중을 위해 생략했다. HTML 글꼴 메트릭은 Flutter와 완전히 같지 않다.
- 별도 입력창 대신 원문 위의 투명 입력 요소를 사용한다. 다음 글자에 선명도 예외를 주지 않는다.

## 검증 기준

100ms 간격 8정답의 최종 내부 너비 43, alpha 19, 점수 22. 이어 오타는 너비 1, alpha 17, 점수 15. 삭제는 1/20/10. 1100ms의 다음 정답은 2.5/23/11. TypeScript와 Rust가 같은 수치를 만족해야 한다.

이전 초안의 시간제·콤보·FLOW 게이지 테스트는 원작 검증이 아니므로 교체했다. 새로운 검증은 원본 수치, UI의 실제 가림, 완주, IME, 다국어 정적 HTML, 두 브라우저 대전 흐름을 다룬다.

## 다음 개선의 우선순위

원작이 기준이다. 새 난이도나 연습 모드는 Classic 기록과 분리하여 별도 승인 후 추가한다. 대전의 공정성은 지연 보정과 자동 입력 대응이 추가 연구 대상이다. 서버 판정은 클라이언트 점수 위조를 막지만 사람이 직접 입력했다는 사실까지 증명하지 못한다. 단말의 발열·통신·Android 프로세스 생존성과 실제 동시접속 부하도 운영 데이터로 검증해야 한다.
