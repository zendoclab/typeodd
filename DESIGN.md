# Typeodd 디자인 원칙

## 게임의 정체성

Typeodd는 속도·단기 기억·긴장감의 균형을 다루는 마인드게임이다. 느리면 문장이 흰 덮개에 희미해지고, 빠르면 커지는 마킹이 앞으로 입력할 글자를 가린다. 마킹 너비가 점수 보상까지 결정한다. 화면을 항상 읽기 쉽게 만드는 것은 게임 규칙을 훼손한다.

## 게임에 바로 도착하기

- 첫 화면은 간단한 브랜드, 솔로/1:1 선택, 지문 언어와 게임판으로 구성한다.
- 초기 알람, 도움말 모달, 홍보 히어로, 기능 카드, 반복적인 소개 섹션은 넣지 않는다.
- 자세한 규칙·제작 배경·FAQ·개인정보는 별도 주소에서 읽게 한다. SEO용 설명을 게임 앞에 길게 쌓지 않는다.
- 조작은 명확하게, 게임 속 가림은 의도대로 유지한다. 진행도·점수·상대 진행률은 지문 밖에 둔다.

## 두 개의 독립된 시각층

1. 원문: 앞에서 맞힌 글자를 제거하여 남은 글자가 앞으로 당겨진다.
2. 흰 덮개: 0–255 불투명도로 원문 대비를 감소시킨다. Gaussian blur로 대체하지 않는다.
3. 회색 마킹: 원문 위를 실제로 덮는 거의 불투명한 직사각형이다. 다음 글자를 항상 보이게 만들거나 그림자로 대체하지 않는다.

원작의 16px 글자, 2px 자간, 원문 시작점 대비 커서의 2px 세로 오프셋을 기준으로 한다. 표시 너비는 내부 너비가 2 미만이면 1px, 그 외에는 1.5배이다. CSS 전환으로 규칙의 시간 반응을 완화하지 않는다. 웹 글꼴과 Flutter 기본 글꼴의 자폭 차이는 남아 있으므로 픽셀 단위로 완전히 동일한 렌더링을 주장하지 않는다.

## 색과 위계

| 용도           | 색                          |
| -------------- | --------------------------- |
| 페이지         | `#f6f5f2`                   |
| 게임 지문 바탕 | 원작의 `#f1e4e8`            |
| 지문           | 원작의 `#4a484d`            |
| 저속 마킹      | `rgba(180,180,180,245/255)` |
| 고속 마킹      | `rgba(100,100,100,245/255)` |
| 흰 덮개        | 흰색, 엔진의 alpha/255      |
| 조작 강조      | 연보라, 시작 버튼의 연두    |

전체 배경의 원작 랜덤 색상과 확대 지문은 필수 판정 규칙이 아니다. 현재 화면에서는 사용자의 게임 집중 요청에 따라 생략한다. 점수와 마킹을 과장하는 흔들림·폭죽·깜빡임은 추가하지 않는다.

## 공정한 도전

- 기본은 Classic이며 시간제·콤보 배수·안개 해제·일시정지를 넣지 않는다.
- 솔로는 전체 지문을 완주하면 종료한다. 같은 지문 재시작과 다음 지문을 구분한다.
- 1:1은 동일 지문·동시 시작·동일 규칙을 사용한다. 먼저 완주한 서버 판정으로 승패를 정한다. 운영상 15분이 지나면 승자 없이 종료한다.
- 대전에서 미리보기와 가림 해제는 제공하지 않는다. 브라우저 DOM 조작이나 자동 타이핑을 완벽하게 방지한다고 주장하지 않는다.
- 소리는 선택 사항이며 기본은 꺼짐이다. 조합 중인 한글은 확정 전까지 감점하지 않는다.

## 다국어와 접근성

화면 언어와 지문 언어는 별개다. 브라우저 언어 자동 선택, 명시적 언어 URL, 수동 선택을 지원한다. 현재 표시 언어는 한국어·영어·일본어·중국어·스페인어이며 지문은 영문·한글이다. 비지원 표시 언어는 영어로 대체한다.

버튼과 메뉴는 읽기 쉬운 대비와 키보드 포커스를 갖춘다. 스크린리더에는 입력 대상 지문을 제공한다. 이는 시각적 마킹 난이도와 동등한 경험이 아니므로 접근 수단 간 경쟁 공정성을 보장한다고 표현하지 않는다. 움직임 줄이기 설정은 장식에 적용하며 게임 판정은 바꾸지 않는다.

## 검증

원작 입력 기록의 너비·불투명도·점수 비교, TypeScript/Rust 동일 기록 비교, 브라우저 실제 마킹 위치와 너비, 한글 조합 중복 이벤트, 모바일 넘침, 두 브라우저의 매칭→진행→승패를 확인한다. 규칙 변경에는 버전을 부여하며 기존 기록과 섞지 않는다.
